type Dimesion = {
  margin: { top: number, right: number, bottom: number, left: number };
  width: number;
  height: number;
};

// type VectorFormat = {
//     { coord1: number, coord2: number },
//     { coord1: number, coord2: number },
// }
